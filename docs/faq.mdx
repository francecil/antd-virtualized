---
name: FAQ
route: /link
order: 0
sidebar: true
---

## npm link 步骤

* 在`antd-virtualized`项目里执行`npm link`
* 在 本地测试项目 里执行`npm link antd-virtualized`

注意：需要用link的时候要删除 package.json 的依赖，如：`npm remove antd-virtualized`，要不会引发更改不生效。


