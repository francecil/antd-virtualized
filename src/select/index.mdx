---
name: Select 选择器
menu: Components
route: /components/select
order: 80
---

import { Playground, PropsTable } from 'docz';
import Select from './index.tsx';
const data = Array.from({ length: 2000 }).map((v, i) => ({ id: i, name: 'test' + i }));

# Select 选择器

下拉选择器。

## 基础展示

引入组件
```
import { Select } from 'antd-virtualized';
```
##### 基础展示

<Select
  placeholder="支持大数据的Select"
  labelKey='name'
  options={data}
  valueKey='id'
  style={{ width: 120 }}
/>

## 基本


### API

### Select